# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 컨텍스트예요.

## 프로젝트 개요

**치이카와 모브의 숲** (줄여서 **모브숲**) — 치이카와 세계관에서 이름 없는 배경 캐릭터를 부르는 말인 "모브"에서 따온 익명 팬 커뮤니티.

- 정식 명칭: 치이카와 모브의 숲 / 짧게 부를 땐 모브숲
- 로그인 없이 누구나 "모브" 하나로 존재하는 게시판
- 글쓴이 식별 없이, 가볍게 목격담/잡담을 흘려놓고 가는 톤
- 비공식 팬 커뮤니티 — 사이트 소개 문구에 "비공식 팬 커뮤니티"임을 명확히 표기할 것. 공식 로고·서체는 사용하지 않음
- 포트폴리오 겸 개인 프로젝트 — 프론트엔드 역량 + 백엔드/인프라(DB 설계, 배포 파이프라인) 역량을 같이 보여주는 게 목적

## 기술 스택

- **Next.js** (App Router) + **TypeScript**
- **Tailwind CSS** (+ 필요 시 shadcn/ui 컴포넌트)
- **Supabase** — Postgres + RLS(Row Level Security)로 익명 쓰기 제어
- **Vercel** — 배포

## 핵심 기획 원칙

### 익명성 처리
- 계정/로그인 없음. 브라우저에 랜덤 토큰(익명 세션 ID)을 저장해서 "본인이 쓴 글"만 수정·삭제 가능하게 함
- 화면에는 어떤 식별 정보도 노출하지 않음. 글마다 "형용사 + 모브 + 랜덤 숫자 3자리" 조합의 닉네임을 부여 (예: 지나가던 모브 #482, 쭈굴 모브 #017, 구석 모브 #930, 버티는 모브, 몰래 모브, 얼떨결 모브, 몽글 모브, 눈치보는 모브, 배고픈 모브, 잠온 모브, 후드쓴 모브, 오늘도 모브 — 이 형용사 풀에 숫자만 붙는 방식)
  - 숫자는 완전한 유일성 목적이 아니라, 댓글 스레드 안에서 "동일 인물 여부"를 구분하기 위한 용도. 그래서 순차 ID가 아니라 랜덤 3자리 정도로 충분함
  - 형용사 단어 목록은 `lib/mobNames.ts` 같은 곳에 배열로 관리하고 계속 추가 가능하게 열어둘 것
  - **[결정됨] 세션 단위 유지** — 익명 세션 하나에 닉네임을 고정해서, 같은 사람이 쓴 글은 같은 닉네임으로 노출 (스레드 내 동일인 구분 목적). 매 글마다 새로 뽑지 않음
- 서버 로그에는 스팸 방지용 익명 세션 ID + 시간만 남기고 그 이상 수집하지 않음

### MVP 범위 (1단계만 먼저 구현)
1단계에서 반드시 포함:
- 익명 글쓰기 (카테고리: 목격담 / 잡담 / 짤·밈)
- 공감(반응) 버튼
- 신고 버튼 — 모더레이션 최소 장치이므로 처음부터 필수

1단계에서 **제외** (2~3단계로 미룸):
- 댓글·대댓글
- 익명 쪽지
- 검색, 인기글 정렬

범위를 넘어서는 기능 제안이 들어오면, 먼저 "이거 1단계 범위 맞아?"를 확인할 것.

### 백로그 — 토벌일지 탭 (2단계 예정)
치이카와의 "토벌" 컨셉을 차용한 별도 탭. 1단계 배포 완료 후 착수하기로 확정.
- **성격**: 실제 다녀온 장소·나들이 경로를 기록/공유하는 탭 (세계관 롤플레이 아님)
- **글쓰기**: 사용자가 글 작성 가능 — 다녀온 곳, "어디서 사면 좋다" 같은 팁 공유
- **지도**: **정적 지도 이미지 + 핀/설명** 방식. 구글맵 등 인터랙티브 지도 API는 쓰지 않음(가볍게 가기로 결정)

### 게시 방식 (결정됨)
**자동 게시** — 글을 쓰면 즉시 노출된다(`posts.status` 기본값 `published`). 별도 관리자 승인 단계는 두지 않고, 문제 글은 신고 버튼으로 받아 사후에 숨김(`hidden`) 처리하는 사후 모더레이션 방식. 승인제(사전 검열)는 트래픽이 커지고 스팸이 실제로 발생하면 2~3단계에서 재검토.

## 코딩 컨벤션

- 변수/함수명: 영어, `camelCase` / `PascalCase` / 상수는 `UPPER_SNAKE_CASE`
- 커밋 메시지: Conventional Commits 형식 + 한글 요약 — `<type>(<scope>): <한글 요약>`
  - 예: `feat(post): 익명 글쓰기 폼 구현`
- 변경은 **최소 범위로**: 요청받지 않은 리팩토링이나 관련 없는 파일 수정은 하지 않음

## Git 워크플로우

- **`git commit`, `git push`는 항상 사용자 확인 후에만 실행.** Claude Code가 자율적으로 커밋/푸시하지 않음
- 변경 사항은 먼저 diff로 보여주고, 커밋 여부는 사용자가 결정

## 폴더 구조 (제안 — 프로젝트 시작 시 확정)

```
/app
  /post/[category]     # 카테고리별 목록
  /api/posts           # Route Handlers
/components
/lib
  /supabase            # 클라이언트, RLS 관련 쿼리
/types
```

## 현재 상태

기획 단계. 코드베이스는 아직 없음. 다음 작업은 Next.js 프로젝트 스캐폴딩 + Supabase 스키마 설계.
